#!/bin/sh

set -eu

repo_root="$(git rev-parse --show-toplevel)"
msg_file="${1:-}"

if [ -z "$msg_file" ]; then
  echo "commit-msg: missing commit message file path" >&2
  exit 1
fi

python "$repo_root/scripts/check_commit_format.py" "$msg_file"
