name: cd

on:
  workflow_dispatch:

jobs:
  deployment-placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment placeholder only
        run: |
          echo "Manual placeholder only."
          echo "This workflow intentionally performs no Docker build, push, or deploy side effects."
          echo "Use ci.yml for required code-health gates."
