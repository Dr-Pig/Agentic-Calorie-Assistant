name: merge-governance

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  merge-governance-advisory:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      PYTHONUTF8: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch branch refs for drift checks
        run: git fetch origin '+refs/heads/*:refs/remotes/origin/*' --prune

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Build merge governance advisory report
        run: python scripts/merge_governance/build_merge_governance_advisory.py --skip-diff-scan --limit 40

      - name: Upload advisory merge governance report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: merge-governance-advisory
          path: |
            artifacts/merge_governance_advisory.json
            artifacts/merge_governance_advisory.md
          if-no-files-found: error
