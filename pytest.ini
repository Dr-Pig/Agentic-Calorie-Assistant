[pytest]
addopts = --basetemp=.pytest_tmp_local
markers =
    smoke: fast deterministic or low-dependency regression coverage required on every PR
    integration: repo-level integration coverage that may use DB or broader runtime assembly
    e2e: long-running or externally coupled coverage kept out of the main CI path
    asyncio: async test marker
