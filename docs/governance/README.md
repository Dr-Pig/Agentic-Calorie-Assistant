# Governance Docs

This folder contains owner docs for repo process, editing discipline, task protocol, handoff protocol, file placement, layer rules, and execution guardrails.

These docs are conditional reads.

Do not treat this folder as default bootstrap truth unless the task explicitly touches repo governance or process rules.
