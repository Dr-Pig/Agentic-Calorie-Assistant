# Handoff References

This directory contains stable operator reference docs for runtime context and historical cleanup notes.

These files are not the default task queue.

Use:

- `docs/handoff/active/` for current task-level operational handoff
- `docs/handoff/completed/` for historical task-level handoff
- `docs/handoff/reference/` for stable handoff reference material
